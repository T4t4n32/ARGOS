# =============================================================================
#  ARGOS — Dependencias Python para Raspberry Pi 5
#  Instalar con:  pip install -r deploy/requirements_pi.txt
# =============================================================================

# ── Core ──────────────────────────────────────────────────────────────────────
PyYAML>=6.0
pydantic>=2.6
rich>=13.7.0

# ── Hardware / GPIO ───────────────────────────────────────────────────────────
# lgpio es el driver GPIO oficial para Raspberry Pi 5 (sustituye a RPi.GPIO)
lgpio>=0.2.2.0
smbus2>=0.4.3           # Comunicación I2C (PT100 MAX31865)
pyserial>=3.5           # Puerto serie para LoRa

# ── Visión ────────────────────────────────────────────────────────────────────
# OpenCV sin GUI (headless) — más liviano en Pi
opencv-python-headless>=4.9.0.80

# YOLOv8 CPU (no instalar torch con CUDA en Pi)
ultralytics>=8.2.0

# Torch CPU — usa la build oficial de ARM; si falla, instala manualmente:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
torchvision>=0.17.0

# ── Comms / Red ───────────────────────────────────────────────────────────────
websockets>=12.0        # Base station server

# ── Testing (solo dev) ────────────────────────────────────────────────────────
# pytest>=8.0.0         # Descomenta para instalar en el Pi (no necesario en prod)
